export type ResponseStatus = 'action' | 'error' | 'ok' | 'actionsList'

export enum APP_STATUS {
    beforeInition = 'BEFORE_INITION',
    ready = 'READY',
    loading = 'LOADING',
    error = 'ERROR'
}
